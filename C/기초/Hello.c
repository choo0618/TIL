// 헤더파일 선언문
#include<stdio.h>

// int(출력형태) main(함수이름) (입력형태)
int main(void) {
	/*
	printf("Hello World!\n");
	printf("%d+%d=%d\n", 1, 2, 1 + 2);
	// 함수를 출력, 함수의 종료
	num(); 
	AndOrNot();
	*/
	abc();

	return 0;
}