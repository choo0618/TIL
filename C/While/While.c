#include<stdio.h>

int main(void) {
	/*
	int num = 0;
	while (num < 5) {
		printf("Hello world!, %d\n", num);
		num++;
	}
	*/

	/*
	// 구구단
	int dan = 0, num = 1;
	printf("몇 단?");
	scanf_s("%d", &dan);
	while (num < 10) {
		printf("%d * %d = %d\n", dan, num, dan * num);
		num++;
	}
	*/

	/*
	int num = 0;
	do {
		printf("Hello world!, %d\n", num);
		num++;
	} while (num < 5);
	*/

	/*
	int total = 0, num = 0;
	do {
		printf("정수 입력(0 to quit): ");
		scanf_s("%d", &num);
		total += num;
	} while (num != 0);
	printf("합계 : %d\n", total);
	*/

	return 0;
}